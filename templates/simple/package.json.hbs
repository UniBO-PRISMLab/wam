{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "thing.js",
  "scripts": {
    "debug" : "wot-servient --inspect-brk ./src/thing.js"
  },
  "keywords": [
    {{#each keywords}}
      "{{this}}"{{#unless @last}},{{/unless}}
    {{/each}}
  ],
  "author": "{{author}}",
  "license": "{{license}}",
  "devDependencies": {
    "@node-wot/cli": "^0.7.0-SNAPSHOT.2",
    "wade": "{{selfVersion}}"
  },
  "dependencies": {
    "@node-wot/core": "^0.7.0-SNAPSHOT.2"
  }
}
